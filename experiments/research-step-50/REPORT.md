# Research step 50: five fixed studies and the decisions they can change

All five research lines have a fixed package for their next cloud step. This
audit checks their actual local archive bytes, published hashes, plans and
bound host files. It also repairs a launch gap shared by the older Solomon
and weight tools. All five free provider checks now pass, and all five packages have verified
readback from private storage. The next scientific results will come from
these fixed studies after package-specific launch approval.

The shared question is **where does learned state earn its full cost on
unfamiliar inputs?** Each project isolates a different place where a learned
component might help. Keep its scientific measure and independent unit clear.

## Current work and decisions

| Project | Fixed next study | Decision the result can change |
| --- | --- | --- |
| [Reasoner](../research-step-46/REPORT.md) | Four methods on 128 fresh families, four views per family and twelve passes; 48 workers and 2,048 distinct measured results. | The full task guide advances when it beats both lexical guidance and semantic frequency under the fixed CPU and verifier-work rules. A further failure directs work toward a different guidance mechanism. |
| [FERAL](../research-step-49/REPORT.md) | Calculator v1, calculator v2 and the operand control on 228 questions from three 2025 company reports. Twelve passes produce 684 distinct results. | Establish numeric coverage and correct abstention for canonical and varied wording. The measured gap sets the target for fact and operation selection in the v3 tagging work. |
| [Solomon / NSRL](../research-step-38/REPORT.md) | Five confidence methods on 384 windows from twelve documents, with five passes and 300 workers. | Compare native confidence with simple probability controls at fixed chosen bytes. Use exact Brier error by document and complete inference cost to choose the next confidence design. |
| [Weight multiplicity](../research-step-35/REPORT.md) | Generate the corpus with the original candidate distribution, both fixed oracles and the published resource policy. | A complete verified corpus that passes every resource gate supplies the later transfer study. A resource Hold retains the partial corpus and identifies the limiting query or cost. |
| [ZERO.4](../research-step-45/REPORT.md) | Five training methods and three seeds, with complete-artifact task scoring, 81 retention windows, BLiMP and TinyStories endpoints. | Test whether guarded replay or projection improves retained operation routing while every source and both language screens stay within the fixed limits. |

Reasoner's CPU point ratio must be at most 0.95 against each reference. Its
one-sided 98.75% CPU upper bound and verifier-work upper bound must each be
below one. The family resampling keeps the four views together; repeated
passes measure variation within the fixed machine and run.

FERAL v2 must gain at least ten percentage points of company-averaged numeric
coverage over both controls in each wording style. Wrong numeric answer counts
must stay at or below each control, and all 64 required abstentions must be
correct. Source spans, labels, arithmetic, repeated rows and complete costs
must verify. This is the next CPU control comparison in the FERAL program.
The earlier GPU cache failure and closed capacity window remain recorded in
[step 28](../research-step-28/REPORT.md) and [step 30](../research-step-30/REPORT.md).
A further model run starts with its own capacity window and exact package.

Solomon's study is a fixed-panel confidence diagnostic. Scores average within
documents before equal document weighting; linked document groups and fiction
versus nonfiction stay visible. Its current artifact's answer-selection audit
makes confidence the quantity that this comparison can change.

ZERO.4 requires each candidate to gain at least 25 exact artifacts out of 500
against both the frozen model and plain replay in every seed. Each of six
source mean losses may grow by at most 1.5%; BLiMP accuracy may drop by at most
1.5 percentage points; TinyStories bits per byte may grow by at most 1.5%.
Rejected state changes must be zero, and every arm and endpoint must finish.
The exact parser and kernel remain the complete reference for this task grammar.

Weight multiplicity keeps its original resource policy, including the final
50 ms p99 gate. The next run is corpus preparation. The learned transfer claim
follows the separate registered training and evaluation study.

## What earlier failures now tell us

- **Reasoner:** the completed comparison missed its benefit gates. The opened
  cost diagnostic located scoring overhead, and eligible scoring cut prior
  calls by 99.02%. Its opened verifier work still exceeds both simple references.
  The fresh four-method run will test whether the revised mechanism earns its cost.
- **FERAL:** v2 answers 21/21 canonical numeric questions and 0/21 paired
  paraphrases in the invented fixture. Exact arithmetic works once facts are
  selected. Wording and fact selection are the next testable limits.
- **Solomon:** native confidence has Brier error 0.702257 on the sixteen opened
  windows, compared with 0.436361 for empirical suffix counts. All five methods
  choose the same bytes. The fresh comparison tests this confidence gap under
  document shift.
- **ZERO.4:** Q2.9 kept its replay measure within its rule and failed its
  language gate. A later synthetic check improved the mean while dialogue
  loss rose by 4.22%. The next study keeps source-level and language gates
  beside task accuracy.
- **Weight multiplicity:** the corpus pilot reached a resource Hold. The
  recovered 26,624-query calibration trace now verifies. The full corpus
  controller keeps partial work and applies the original resource gates.

Each finding keeps its original scope in the linked project reports. The
opened examples guide interpretation of the fixed fresh studies. Future
changes to a method receive a new comparison after those results are recorded.

## One shared launch repair

The audit found that the older Solomon and weight launchers accepted a
preflight for another run ID. That reused the output-prefix check from that
other run. The controlled reproduction reached one fake EC2 request in each
launcher. Actual instances created were zero.

Both preflight tools now write the run ID, and both launchers require the
receipt to match it. The common test covers all five projects. Its fifty
cases cover a matching launch, changed or missing run ID, changed package or
version, old or future preflight, changed budget, and a failed provider check
that retains the run identity. All seven test methods pass. The ten weight
host tests and twelve Solomon host tests pass too.

[FAILURES.json](FAILURES.json) binds the before and after records. The repaired
local launch and preflight tools sit outside the Solomon and weight host
archives. All five host archives retain their published bytes and budgets.
[PACKAGE-AUDIT.json](PACKAGE-AUDIT.json) records each archive, source commit,
plan hash, current local tool hash and earlier provider-check record.

## Current packages and cost

| Project | Package | Instance limit | Cost ceiling before tax |
| --- | --- | ---: | ---: |
| Reasoner | [step 46](../research-step-46/PACKAGE.json), `080ed8f9…` | 90 minutes | $0.30 |
| FERAL coverage | [step 49](../research-step-49/PACKAGE.json), `c392e3a5…` | 30 minutes | $0.15 |
| Solomon / NSRL | [step 38](../research-step-38/PACKAGE.json), `52dfcf2f…` | 60 minutes | $0.25 |
| Weight corpus | [step 35](../research-step-35/PACKAGE.json), `baca1320…` | 90 minutes | $2.00 |
| ZERO.4 | [step 45](../research-step-45/PACKAGE.json), `0ed0d960…` | 13.5 hours | $12.00 |

The sum of these five individual ceilings is **$14.70 before tax**. Each
package retains its own limit, approval and result collection. Current
provider prices must fit the fixed ceilings at launch preflight. The September 12 sign-in renewal succeeded.
The [new provider checks](PREFLIGHT.json) verify all seven price components
against each ceiling, machine and image identity, network and role settings,
private storage, fixed object versions, bounded expiry and EC2 launch permission.
All five checks used dry-run requests; actual instances created were zero.
Repeat a preflight when its receipt is over one hour old at launch.

My proposed order is Reasoner, FERAL coverage, Solomon, weight corpus, then
ZERO.4. This puts the direct mechanism and cheap control questions first,
followed by corpus preparation and the longest retention run. Each study's
published methods and outcome rules stay fixed.

[RUN-SET.json](RUN-SET.json) lists the exact five packages, run identities,
local launch-tool source commit and individual ceilings for budget review.
The proposed total is $14.70 before tax, with one bounded run per package.
After approval, run them step by step, collect from immutable storage, verify
termination and cleanup, then publish each result and failure before choosing
the following scientific change.

Staging added twelve expiry rules for the three new packages and result
prefixes. All eight earlier rules and provider settings remain identical.
The first response check included an extra provider metadata field and failed
a whole-object comparison. Comparing the submitted rules and retained metadata
separately resolved the check. That failure is preserved in FAILURES.json.
The full local schema suite and source CI pass; RESULT.json binds their scope.

The final source-kit CI check also recorded a public-input download timeout
before compilation. Its unchanged second attempt passed. The first log and
retry identity remain in FAILURES.json and the private evidence folder.
