# ZERO.4: explicit retention windows

Preparation is in progress. The plan fixes separate replay and endpoint
windows at the existing 512-token model context. The guard uses a replay
example. The final retention check uses separate text. Selection decisions,
native parity, and every failed attempt will follow.
