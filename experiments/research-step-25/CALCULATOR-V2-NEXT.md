# Calculator v2: next coverage test

Freeze v2 at the source hashes in CALCULATOR-V2-SMOKE.json. The five opened
cases are development checks. The next comparison needs a separately frozen
fresh roster before grading.

Use the same supplied evidence and target policy for v1, v2, and v2 operand-only.
Draw the roster from company reports outside the opened 1,147-case FinQA set.
Record report hashes, selection rules, question authorship, numeric units,
answerability, and target hashes before execution. Keep target fields outside
predictor inputs. Include each supported question form and ambiguous or
insufficient evidence. Freeze the roster size and family split after source
intake, then freeze the paired analysis and cost limits before a full run.

Measure numeric coverage, exact numeric accuracy with the fixed unit policy,
correct abstentions, wrong numeric answers, and total process cost. Retain
per-case evidence spans and every abstention reason. Report each question form
and company separately so added coverage can be traced to its source.

This document fixes the comparison structure. The fresh source intake, roster,
and final scoring contract remain the next deliverable. The paid baseline
package in this step contains the original v1 controls.
